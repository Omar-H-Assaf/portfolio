import { styled } from "styled-components";

export const HomeH1 = styled.h1({
    fontSize: '7rem',
    fontWeight: '900',
    color: '#EBEBEB'
})

export const SpanDot = styled.span({
    color: '#65d2a3'
})